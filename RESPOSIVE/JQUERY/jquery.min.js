$.ajax({ type: 'Get' , url :' https://jsonplaceholder.typicode.com/posts' }) .  done( function( data , status) {

for(i=0;i<data.length;i++){
    
   x=  $("body").append(`<p class =del${i}>${data[i].title}</p`).append(`<a href="./form.html"><button class =del${i} id="${i}">edite${i}</button><a>`).click(fun).append(`<button class =del${i} id=${i} >delete</button>`).click(del)
}
function fun (e){
    localStorage.setItem("user", JSON.stringify(e.target.id))
       
    
}


} ).fail(function(error){

})
function del (e){
        var x='del'+e.target.id
        $(`.${x}`).hide()
}
